重点掌握：
1.字符串截取方法slice,substr,indexOf
2.字符串拼接和分割方法：concat,split
3.字符串字母大小写转换：toLowerCase,toUpperCase
4.字符串替换方法：replace
搞清楚这些方法的功能，参数，返回值


程序题：
1、判断一个字符（比如"k"）是否出现在另一个字符（比如"23k40skdfje2kffk2"）中，
如果出现，并统计其出现的次数。

2、查找字符串"85268068276"中的"6"，打印出起下标位置，并将其从元字符串中去除。

3、从页面input框中获取到输入的英文字符串，将其存到变量s1中，
将该字符“反转顺序”并首尾字母转为大写，其他字母转为小写后输出到页面。

4、获取一个url路径（比如”https://www.example.cn/day2/news.html”）中的
协议部分，域名部分，还有文件后缀。
提示:协议分有https/http/ftp；域名部分：包含www和不包含www；
文件后缀:有后缀和没有后缀的情况，详细演示看GIF动图效果

5、将字符串” 49238028424”以“2”为分隔符分割为数组，并求出数组的长度。

6.代码实现一个对字母和中文进行Base64编码和解码。
提示：界面效果如Unicode转码效果。

7、将如下字符串转为驼峰命名法:
var str = get-elements-by-class-name;
转换后为：getElementsByClassName

