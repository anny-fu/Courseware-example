程序题：
1.（Date）获取当前系统时间，然后格式化为：YYYY-MM-DD的形式输出到页面。
2.（Math）求圆周率PI的5次方，并四舍五入取整。
3.（Math，逻辑）写一段程序，要求每次刷新页面，就会在页面中显示6个两位数的整数，此六个数不能有重复。
4.（Math，逻辑）编写一个函数，带一个参数n，在页面上输出1～n（n>1）之间所有能同时被3和5整除的偶数，并要求每行只输出6个（即超过6个后要换行再输出）。