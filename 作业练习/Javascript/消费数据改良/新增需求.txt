新增需求：
1.之前是通过对象数组的方式存储的数据，现改为数组对象的方式进行数据存储。
2.去掉重置按钮，在点击“添加”按钮执行完数据显示后将所有的input清空。
3.input标签在获取焦点(onfocus)的时候，动态给label标签添加class属性（
样式写在一个单独的类选择器中,样式大家可以自行添加，只要能达到样式改变效果即可）
，失去焦点(onblur)的时候，删除这个class属性。